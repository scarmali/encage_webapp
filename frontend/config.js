// enCAGE frontend config
// Point this at your deployed Render (or Railway) backend URL, no trailing slash.
// Local testing against `python app.py`: use "http://localhost:5000"
window.ENCAGE_API_BASE = "https://YOUR-BACKEND-NAME.onrender.com";
